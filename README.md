# مكتبي — Maktabi

واجهة عربية أولاً لإدارة أعمال مكاتب المحاماة السودانية.

## التشغيل

```bash
npm install
npm run dev
```

توجد نماذج المجال في `packages/domain`، ونظام التصميم في `packages/ui`، وتطبيق الويب المتجاوب في `apps/app`.
